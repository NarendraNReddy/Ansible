- name: install and run nginx
  hosts: web
  become: yes #root access 
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
     name: httpd
     state: latest
  - name: run nginx
    ansible.builtin.service:
     name: httpd
     state: started 
     enabled: yes

