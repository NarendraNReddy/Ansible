- name: install and run nginx
  hosts: web
  become: yes #root access 
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: httpd
      state: latest
  -name: start nginx
    ansible.builtin.service:
      name: start nginx
      state: started
      enabled: yes

